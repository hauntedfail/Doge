# Backlog

- Choose the final `h1ka.ru` subdomain and configure the production origin.
- Add Cloudflare Tunnel and an authentication policy suitable for the Even Hub WebView.
- Validate the live Safe Relay flow against the signed-in X account.
- Run the packaged app on physical Even G2 hardware.
